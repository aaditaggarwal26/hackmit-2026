.venv/
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
.DS_Store
*.png
!viz/static/**/*.png
.coverage
!corpus/png/*.png
!results/filtered_vs_fifo.png
# the project page ships its frames next to it, so it needs no build step
!site/frames/*.png
results/raw/
runs/*
!runs/sample.jsonl
!runs/demo-*.jsonl

# the WiFi credentials, the ground hostname and the pre-shared control-bus key live here
firmware/satellite_esp32/secrets.h

# the host harnesses g++ builds out of firmware/test/*.cpp, per the commands in their headers
firmware/test/*_host
firmware/test/*.o
